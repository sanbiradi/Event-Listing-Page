import logo from "../images/azent-logo.png";

const Header = () => {
  return (
    <nav>
      <div className="container">
        <img src={logo} alt="" />
      </div>
    </nav>
  );
};

export default Header;
